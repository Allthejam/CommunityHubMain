
// This layout is being simplified to remove the dedicated police header,
// as the role is being integrated into the standard leader dashboard.

export default function PoliceRedirectLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
